PORT=4000
# Use Supabase "Session pooler" URI from Dashboard → Database → Connect (not the direct db.* host if IPv6 fails)
# Example: postgresql://postgres.[ref]:[password]@aws-1-ap-south-1.pooler.supabase.com:5432/postgres?sslmode=require
DATABASE_URL=
JWT_SECRET=dev-only-change-me-dev-only
SUPABASE_URL=
SUPABASE_SERVICE_ROLE=
SMS_API_KEY=
SMS_SENDER_ID=
SMS_PROVIDER=mock
SUPER_ADMIN_EMAIL=admin@mahadev.local
SUPER_ADMIN_PASSWORD=ChangeMe@12345

# Password reset emails (mock logs link to console; use smtp for production)
APP_URL=http://localhost:5173
EMAIL_PROVIDER=mock
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=

